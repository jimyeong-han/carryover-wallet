<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/widget_bg_img"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:src="@drawable/widget_bg_shape"
        android:contentDescription="@null" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:padding="12dp">

        <!-- 상단: 라벨/잔액 + 우측 가로 콘텐츠 + 빠른추가 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/widget_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="오늘 쓸 수 있는 돈"
                    android:textColor="#B0FFFFFF"
                    android:textSize="12sp"
                    android:shadowColor="#80000000"
                    android:shadowRadius="3" />

                <TextView
                    android:id="@+id/widget_balance"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:text="-"
                    android:textColor="#FFFFFF"
                    android:textStyle="bold"
                    android:textSize="22sp"
                    android:shadowColor="#80000000"
                    android:shadowRadius="3" />
            </LinearLayout>

            <!-- 우측 가로 콘텐츠 -->
            <LinearLayout
                android:id="@+id/sec_right"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="10dp"
                android:gravity="center_vertical|end"
                android:orientation="horizontal"
                android:visibility="gone">

                <LinearLayout
                    android:id="@+id/rc_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:gravity="end">

                    <TextView android:id="@+id/rc_label" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="하루 가능" android:textColor="#B0FFFFFF" android:textSize="11sp" android:shadowColor="#80000000" android:shadowRadius="3" />
                    <TextView android:id="@+id/rc_value" android:layout_width="wrap_content" android:layout_height="wrap_content" android:layout_marginTop="2dp" android:text="" android:textColor="#FFFFFF" android:textStyle="bold" android:textSize="15sp" android:shadowColor="#80000000" android:shadowRadius="3" />
                </LinearLayout>

                <ImageView
                    android:id="@+id/rc_img"
                    android:layout_width="match_parent"
                    android:layout_height="42dp"
                    android:scaleType="fitXY"
                    android:contentDescription="@null"
                    android:visibility="gone" />

                <!-- 빠른 추가(분류 칩) -->
                <LinearLayout
                    android:id="@+id/rc_quick"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical|end"
                    android:visibility="gone">

                    <TextView android:id="@+id/rc_q_food" android:layout_width="42dp" android:layout_height="40dp" android:layout_marginStart="6dp" android:gravity="center" android:text="🍚" android:textSize="18sp" android:background="@drawable/widget_add_bg_ghost" />
                    <TextView android:id="@+id/rc_q_cafe" android:layout_width="42dp" android:layout_height="40dp" android:layout_marginStart="6dp" android:gravity="center" android:text="☕" android:textSize="18sp" android:background="@drawable/widget_add_bg_ghost" />
                    <TextView android:id="@+id/rc_q_etc" android:layout_width="42dp" android:layout_height="40dp" android:layout_marginStart="6dp" android:gravity="center" android:text="📦" android:textSize="18sp" android:background="@drawable/widget_add_bg_ghost" />
                </LinearLayout>
            </LinearLayout>

            <TextView
                android:id="@+id/widget_add"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_marginStart="10dp"
                android:gravity="center"
                android:text="＋"
                android:textColor="#FFFFFF"
                android:textSize="22sp"
                android:textStyle="bold"
                android:background="@drawable/widget_add_bg"
                android:contentDescription="빠른 지출 추가" />
        </LinearLayout>

        <!-- 하단 A: 진행 바 + 오늘 사용 -->
        <LinearLayout
            android:id="@+id/sec_progress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:orientation="vertical"
            android:visibility="gone">

            <ProgressBar
                android:id="@+id/widget_progress"
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="8dp"
                android:max="100"
                android:progress="0"
                android:progressDrawable="@drawable/widget_progress" />

            <TextView
                android:id="@+id/widget_progress_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="5dp"
                android:text=""
                android:textColor="#B0FFFFFF"
                android:textSize="11sp"
                android:shadowColor="#80000000"
                android:shadowRadius="3" />
        </LinearLayout>

        <!-- 하단 B: 이번 달 요약 -->
        <TextView
            android:id="@+id/sec_summary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:text=""
            android:textColor="#B0FFFFFF"
            android:textSize="12sp"
            android:shadowColor="#80000000"
            android:shadowRadius="3"
            android:visibility="gone" />

        <!-- 하단 C: 최근 지출 -->
        <LinearLayout
            android:id="@+id/sec_recent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="vertical"
            android:visibility="gone">

            <TextView android:id="@+id/widget_rec1" android:layout_width="match_parent" android:layout_height="wrap_content" android:maxLines="1" android:ellipsize="end" android:textColor="#B0FFFFFF" android:textSize="12sp" android:shadowColor="#80000000" android:shadowRadius="3" />
            <TextView android:id="@+id/widget_rec2" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="3dp" android:maxLines="1" android:ellipsize="end" android:textColor="#B0FFFFFF" android:textSize="12sp" android:shadowColor="#80000000" android:shadowRadius="3" />
            <TextView android:id="@+id/widget_rec3" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_marginTop="3dp" android:maxLines="1" android:ellipsize="end" android:textColor="#B0FFFFFF" android:textSize="12sp" android:shadowColor="#80000000" android:shadowRadius="3" />
        </LinearLayout>

    </LinearLayout>
</FrameLayout>
